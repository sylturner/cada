<!-- Shops Section -->
<section id="shops" class="content-section">
<div class="shops-section">
  <div class="container">
    <div class="col-lg-8 col-lg-offset-2" id="shops">
      <h2 class="text-center">Stroll from Shop to Shop</h2>
      <iframe src="https://mapsengine.google.com/map/u/0/embed?mid=zMyhsF3_QU8E.klVYploWvPgw" width="100%" height="100%" class="map-embed img-responsive"></iframe>
      <br/>
      <% data.shops.sort_by{|x| x.first}.each_slice(5) do |slice| %>
      <div class='btn-group-vertical col-xs-12'>
        <% slice.each do |store| %>
          <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#<%= store.first %>"><%=store.last.name%></button>
          <div class="modal fade" id="<%= store.first %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg">
              <div class="modal-content">
                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                  <h4 class="modal-title" id="myModalLabel"><%= store.last.name %></h4>
                </div>
                <div class="modal-body">
                  <p class="address-hours">
                  <span class="glyphicon glyphicon-map-marker" aria-hidden="true"></span> <a target="_blank" href="https://maps.google.com/maps?q=<%= "#{store.last.address.split(" ").join("+")}+#{store.last.zip_code}" %>"><%= store.last.address %></a><br/>
                  <span class="glyphicon glyphicon-phone-alt" aria-hidden="true"></span> <a href="tel:<%= store.last.phone %>"><%= store.last.phone %></a><br/>
                  <span class="glyphicon glyphicon-time" aria-hidden="true"></span> <em>Hours:</em><br/>
                  <%= store.last.hours %>
                  </p>
                  <p><%= store.last.desc %></p>
                  <div class="center-block text-center">
                    <div class="btn-group">
                      <% if store.last.website %>
                        <a class="btn btn-default btn-lg" href="<%= store.last.website %>"><i class="fa fa-globe"></i> Website</a>
                      <% end %>
                      <% if store.last.facebook %>
                        <a class="btn btn-default btn-lg" href="<%= store.last.facebook %>"><i class="fa fa-facebook-official"></i> Facebook</a>
                      <% end %>
                      <% if store.last.twitter %>
                        <a class="btn btn-default btn-lg" href="<%= store.last.twitter %>"><i class="fa fa-twitter"></i> Twitter</a>
                      <% end %>
                      <% if store.last.instagram %>
                        <a class="btn btn-default btn-lg" href="<%= store.last.instagram %>"><i class="fa fa-instagram"></i> Instagram</a>
                      <% end %>
                      <% if store.last.googleplus %>
                        <a class="btn btn-default btn-lg" href="<%= store.last.googleplus %>"><i class="fa fa-google-plus"></i> Google+</a>
                      <% end %>
                    </div>
                  </div>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <%end%>
    </div>
  </div>
</div>
</section>
